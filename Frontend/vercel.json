{  
  "routes":[
                 { "src": "/[^.]+",
                  "dest":"/" }
  ],  "rewrites": [
    { "source": "/locales/:path*", "destination": "/locales/:path*" }
  ],
  "builds": [
    {
      "src": "public/locales/*",
      "use": "@vercel/static"
    }
  ]
}

